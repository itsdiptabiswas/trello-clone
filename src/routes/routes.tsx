import BoardIndex from 'components/board';

const DefaultRoutes = () => {
  return [
    { path: '/', element: <BoardIndex /> },
    { path: '/d', element: <> </> },
  ];
};

export default DefaultRoutes;
